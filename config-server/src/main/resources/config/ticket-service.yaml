server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:parkingdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

management:
  endpoints:
    web:
      exposure:
        include: "refresh,health,info" # We open refresh for updates, health for monitoring
  endpoint:
    health:
      show-details: always # Helpful for debugging database/kafka connections

# Custom property for your business logic (example)
parking:
  maxSpots: 100
  hourlyRates:
    car: 5.0
    motorcycle: 2.0
    truck: 8.0
